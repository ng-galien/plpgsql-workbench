-- Auto-generated by pgm supabase sync
-- Module: purchase@0.1.0
-- Source: modules/purchase/build/purchase.ddl.sql

-- purchase — DDL (Commandes fournisseurs, réceptions, factures fournisseur)

CREATE SCHEMA IF NOT EXISTS purchase;
CREATE SCHEMA IF NOT EXISTS purchase_ut;
CREATE SCHEMA IF NOT EXISTS purchase_qa;
GRANT USAGE ON SCHEMA purchase TO anon;

-- Commandes fournisseur (bons de commande)
CREATE TABLE IF NOT EXISTS purchase.commande (
  id serial PRIMARY KEY,
  tenant_id text NOT NULL DEFAULT current_setting('app.tenant_id', true),
  numero text NOT NULL UNIQUE,                    -- CMD-2026-001
  fournisseur_id int NOT NULL REFERENCES crm.client(id),
  objet text NOT NULL,
  statut text NOT NULL DEFAULT 'brouillon'
    CHECK (statut IN ('brouillon', 'envoyee', 'partiellement_recue', 'recue', 'annulee')),
  notes text NOT NULL DEFAULT '',
  date_livraison date,                            -- NULL = pas de date prévue
  created_at timestamptz NOT NULL DEFAULT now(),
  updated_at timestamptz NOT NULL DEFAULT now()
);

CREATE INDEX IF NOT EXISTS idx_commande_fournisseur ON purchase.commande(fournisseur_id);
CREATE INDEX IF NOT EXISTS idx_commande_statut ON purchase.commande(statut);
CREATE INDEX IF NOT EXISTS idx_commande_tenant ON purchase.commande(tenant_id);

-- Lignes de commande
CREATE TABLE IF NOT EXISTS purchase.ligne (
  id serial PRIMARY KEY,
  tenant_id text NOT NULL DEFAULT current_setting('app.tenant_id', true),
  commande_id int NOT NULL REFERENCES purchase.commande(id) ON DELETE CASCADE,
  sort_order int NOT NULL DEFAULT 0,
  description text NOT NULL,
  quantite numeric(10,2) NOT NULL DEFAULT 1,
  unite text NOT NULL DEFAULT 'u',                -- u, h, m, m2, m3, kg, forfait
  prix_unitaire numeric(12,2) NOT NULL,
  tva_rate numeric(4,2) NOT NULL DEFAULT 20.00
    CHECK (tva_rate IN (0.00, 5.50, 10.00, 20.00))
);

-- article_id: soft FK vers stock.article (pas de hard dependency)
ALTER TABLE purchase.ligne ADD COLUMN IF NOT EXISTS article_id integer;
CREATE INDEX IF NOT EXISTS idx_ligne_article ON purchase.ligne(article_id);

CREATE INDEX IF NOT EXISTS idx_ligne_commande ON purchase.ligne(commande_id);
CREATE INDEX IF NOT EXISTS idx_ligne_tenant ON purchase.ligne(tenant_id);

-- Réceptions marchandises
CREATE TABLE IF NOT EXISTS purchase.reception (
  id serial PRIMARY KEY,
  tenant_id text NOT NULL DEFAULT current_setting('app.tenant_id', true),
  commande_id int NOT NULL REFERENCES purchase.commande(id),
  numero text NOT NULL UNIQUE,                    -- REC-2026-001
  notes text NOT NULL DEFAULT '',
  received_at timestamptz NOT NULL DEFAULT now(),
  created_at timestamptz NOT NULL DEFAULT now()
);

CREATE INDEX IF NOT EXISTS idx_reception_commande ON purchase.reception(commande_id);
CREATE INDEX IF NOT EXISTS idx_reception_tenant ON purchase.reception(tenant_id);

-- Détail réception par ligne
CREATE TABLE IF NOT EXISTS purchase.reception_ligne (
  id serial PRIMARY KEY,
  tenant_id text NOT NULL DEFAULT current_setting('app.tenant_id', true),
  reception_id int NOT NULL REFERENCES purchase.reception(id) ON DELETE CASCADE,
  ligne_id int NOT NULL REFERENCES purchase.ligne(id),
  quantite_recue numeric(10,2) NOT NULL CHECK (quantite_recue > 0)
);

CREATE INDEX IF NOT EXISTS idx_reception_ligne_reception ON purchase.reception_ligne(reception_id);
CREATE INDEX IF NOT EXISTS idx_reception_ligne_ligne ON purchase.reception_ligne(ligne_id);
CREATE INDEX IF NOT EXISTS idx_reception_ligne_tenant ON purchase.reception_ligne(tenant_id);

-- Factures fournisseur (documents reçus)
CREATE TABLE IF NOT EXISTS purchase.facture_fournisseur (
  id serial PRIMARY KEY,
  tenant_id text NOT NULL DEFAULT current_setting('app.tenant_id', true),
  commande_id int REFERENCES purchase.commande(id), -- NULL = facture sans commande
  numero_fournisseur text NOT NULL,               -- numéro sur la facture du fournisseur
  montant_ht numeric(12,2) NOT NULL,
  montant_ttc numeric(12,2) NOT NULL,
  date_facture date NOT NULL,
  date_echeance date,                             -- NULL = pas d'échéance connue
  statut text NOT NULL DEFAULT 'recue'
    CHECK (statut IN ('recue', 'validee', 'payee')),
  notes text NOT NULL DEFAULT '',
  created_at timestamptz NOT NULL DEFAULT now(),
  updated_at timestamptz NOT NULL DEFAULT now()
);

-- Conditions de paiement (ex: "30j fin de mois", "comptant")
ALTER TABLE purchase.commande ADD COLUMN IF NOT EXISTS conditions_paiement text NOT NULL DEFAULT '';

-- Flag anti-doublon comptabilisation
ALTER TABLE purchase.facture_fournisseur ADD COLUMN IF NOT EXISTS comptabilisee boolean NOT NULL DEFAULT false;

CREATE INDEX IF NOT EXISTS idx_facture_fournisseur_commande ON purchase.facture_fournisseur(commande_id);
CREATE INDEX IF NOT EXISTS idx_facture_fournisseur_statut ON purchase.facture_fournisseur(statut);
CREATE INDEX IF NOT EXISTS idx_facture_fournisseur_tenant ON purchase.facture_fournisseur(tenant_id);

-- Triggers updated_at (function deployed via pg_func_set)
DROP TRIGGER IF EXISTS trg_commande_updated_at ON purchase.commande;
CREATE TRIGGER trg_commande_updated_at
  BEFORE UPDATE ON purchase.commande
  FOR EACH ROW EXECUTE FUNCTION purchase._set_updated_at();

DROP TRIGGER IF EXISTS trg_facture_fournisseur_updated_at ON purchase.facture_fournisseur;
CREATE TRIGGER trg_facture_fournisseur_updated_at
  BEFORE UPDATE ON purchase.facture_fournisseur
  FOR EACH ROW EXECUTE FUNCTION purchase._set_updated_at();

-- RLS
ALTER TABLE purchase.commande ENABLE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS tenant_isolation ON purchase.commande;
CREATE POLICY tenant_isolation ON purchase.commande
  USING (tenant_id = current_setting('app.tenant_id', true));

ALTER TABLE purchase.ligne ENABLE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS tenant_isolation ON purchase.ligne;
CREATE POLICY tenant_isolation ON purchase.ligne
  USING (tenant_id = current_setting('app.tenant_id', true));

ALTER TABLE purchase.reception ENABLE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS tenant_isolation ON purchase.reception;
CREATE POLICY tenant_isolation ON purchase.reception
  USING (tenant_id = current_setting('app.tenant_id', true));

ALTER TABLE purchase.reception_ligne ENABLE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS tenant_isolation ON purchase.reception_ligne;
CREATE POLICY tenant_isolation ON purchase.reception_ligne
  USING (tenant_id = current_setting('app.tenant_id', true));

ALTER TABLE purchase.facture_fournisseur ENABLE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS tenant_isolation ON purchase.facture_fournisseur;
CREATE POLICY tenant_isolation ON purchase.facture_fournisseur
  USING (tenant_id = current_setting('app.tenant_id', true));

-- Permissions
GRANT SELECT, INSERT, UPDATE, DELETE ON purchase.commande TO anon;
GRANT USAGE ON SEQUENCE purchase.commande_id_seq TO anon;

GRANT SELECT, INSERT, UPDATE, DELETE ON purchase.ligne TO anon;
GRANT USAGE ON SEQUENCE purchase.ligne_id_seq TO anon;

GRANT SELECT, INSERT, UPDATE, DELETE ON purchase.reception TO anon;
GRANT USAGE ON SEQUENCE purchase.reception_id_seq TO anon;

GRANT SELECT, INSERT, UPDATE, DELETE ON purchase.reception_ligne TO anon;
GRANT USAGE ON SEQUENCE purchase.reception_ligne_id_seq TO anon;

GRANT SELECT, INSERT, UPDATE, DELETE ON purchase.facture_fournisseur TO anon;
GRANT USAGE ON SEQUENCE purchase.facture_fournisseur_id_seq TO anon;

GRANT USAGE ON SCHEMA purchase_ut TO anon;
GRANT USAGE ON SCHEMA purchase_qa TO anon;

-- Default privileges
ALTER DEFAULT PRIVILEGES IN SCHEMA purchase GRANT EXECUTE ON FUNCTIONS TO anon;
ALTER DEFAULT PRIVILEGES IN SCHEMA purchase_ut GRANT EXECUTE ON FUNCTIONS TO anon;
ALTER DEFAULT PRIVILEGES IN SCHEMA purchase_qa GRANT EXECUTE ON FUNCTIONS TO anon;
