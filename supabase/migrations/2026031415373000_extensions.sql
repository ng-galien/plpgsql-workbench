-- Auto-generated by pgm supabase sync
-- Extensions required by: pgv, workbench, asset, document, crm, quote, project, planning, cad, purchase, stock, catalog, expense, hr, ledger, ops

CREATE EXTENSION IF NOT EXISTS unaccent;
CREATE EXTENSION IF NOT EXISTS postgis;
CREATE EXTENSION IF NOT EXISTS postgis_sfcgal;

-- Domain text/html for PostgREST HTML content negotiation
DO $$ BEGIN
  CREATE DOMAIN "text/html" AS TEXT;
EXCEPTION WHEN duplicate_object THEN NULL;
END $$;

