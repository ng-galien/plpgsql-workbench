services:
  agent-docs:
    build: .
    network_mode: host
    stdin_open: true
    tty: true
    volumes:
      - ./.credentials.json:/home/agent/.claude/.credentials.json:ro
      - ../../src/channel:/home/agent/workspace/channel:ro
      - ../../modules/docs/.mcp.json:/home/agent/workspace/.mcp.json:ro
      - ../../modules/docs/CLAUDE.md:/home/agent/workspace/CLAUDE.md:ro
      - ../../modules/docs/.claude:/home/agent/workspace/.claude:ro
    environment:
      MODULE: docs
      PLPGSQL_CONNECTION: postgresql://postgres:postgres@127.0.0.1:5433/postgres
      CHANNEL_PORT: 8790
    command: >
      claude --dangerously-skip-permissions
             --dangerously-load-development-channels server:workbench-channel

  agent-pgv:
    build: .
    network_mode: host
    stdin_open: true
    tty: true
    volumes:
      - ./.credentials.json:/home/agent/.claude/.credentials.json:ro
      - ../../src/channel:/home/agent/workspace/channel:ro
      - ../../modules/pgv/.mcp.json:/home/agent/workspace/.mcp.json:ro
      - ../../modules/pgv/CLAUDE.md:/home/agent/workspace/CLAUDE.md:ro
      - ../../modules/pgv/.claude:/home/agent/workspace/.claude:ro
    environment:
      MODULE: pgv
      PLPGSQL_CONNECTION: postgresql://postgres:postgres@127.0.0.1:5433/postgres
      CHANNEL_PORT: 8791
    command: >
      claude --dangerously-skip-permissions
             --dangerously-load-development-channels server:workbench-channel
